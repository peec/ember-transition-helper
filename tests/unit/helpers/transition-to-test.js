import { transitionTo } from '../../../helpers/transition-to';
import { module, test } from 'qunit';

module('Unit | Helper | transition to');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = transitionTo([42]);
  assert.ok(result);
});
